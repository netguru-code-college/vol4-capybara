
</br>

<%= link_to "My ingredients", ingredients_path %>

<% if current_user && current_user.admin %>
  <%= link_to "Add new ingredient", new_ingredient_path %>
<% end %>

</br></br>


<%= form_with(method: :get, local: true) do |f| %>

  <% @ingredients.each do |ingredient| %>
    <div>
      
      <%= f.check_box(:selected_ingredients,
                      {multiple: true, checked: @selected_ingredient_names.include?(ingredient.product.name)},
                      ingredient.product.name,
                      nil ) %> 
      <%= ingredient.product.name %>
      <%= ingredient.exp_date %>
      <%= ingredient.quantity %>
      <%= ingredient.unit %>

    </div>
  <% end %>

      <%= f.select :Food_category, Recipe.food_cats.map { |key, value| [key.humanize, key] } %>
      <%= f.select :Diet_category, Recipe.diet_cats.map { |key, value| [key.humanize, key] } %>
  <%= f.submit "Send" %>
<% end %>





</br></br>
<%= @all_parameters %>
</br></br>



<!--display api response-->

<% if !@api_response_hash.empty? %>

  <h2>Recommended Recipes</h2>

  <% @api_response_hash['results'].each do |hash| %>

    <%# hash['title'] %>
    <%# hash['href'] %>
    <%# hash['ingredients'] %>
    <%# hash['thumbnail'] %>

    <img src="<%= hash['thumbnail'] %>" height="100" width="100">

    <b>
      <%= hash['title'] %>
    </b>

    <a href="<%= hash['href'] %>">
      LINK
    </a>

    </br>
    <p>All ingredients: <%= hash['ingredients'] %> </p>

    </br>

  <% end %>
<% end %>

    </div>
  </div>












